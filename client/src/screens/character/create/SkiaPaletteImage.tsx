/**
 * SkiaPaletteImage.tsx
 *
 * Placeholder — Skia palette-swap shader approach đã bị bỏ (gây crash runtime).
 * Palette color swap sẽ được xử lý server-side.
 * File giữ lại để không break import, sẽ xóa sau.
 */
export {};
